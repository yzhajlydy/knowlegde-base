# 个人知识库 Wiki — LLM Wiki Schema

语言：中文（界面、注释、callout 均用中文，专有名词保留原文）
模式：混合模式（Research + Personal Second Brain）
目的：系统性整理多领域交叉知识，让阅读和思考持续复利增长
创建日期：2026-06-06

---

## 架构

```
vault/
├── CLAUDE.md              ← 本文件：行为规范
├── raw/                   ← Layer 1: 不可变原始素材
│   ├── books/             书籍笔记/摘要导出
│   ├── cards/             Wolai 知识卡片导出
│   ├── articles/          网络文章
│   └── assets/            图片、PDF
│
├── wiki/                  ← Layer 2: LLM 维护的知识库
│   ├── index.md           主索引（列出所有领域和概念）
│   ├── log.md             时间线日志（新条目追加在顶部）
│   ├── hot.md             热缓存（~500字最近上下文摘要）
│   ├── overview.md        全局概览
│   ├── sources/           每本书/课程的摘要页
│   ├── concepts/          概念、思想、框架（原子化，一概念一页，360个）
│   ├── entities/          人物、组织
│   ├── domains/           领域聚合页（11个，每个概念一句话解释）
│   │   ├── 经济学         市场机制、成本理论、边际思维、价格理论
│   │   ├── 心理学         认知偏差、动机、情绪、行为心理学
│   │   ├── 管理学         团队建设、组织管理、领导力、激励
│   │   ├── 认知科学       思维模式、学习理论、注意力管理
│   │   ├── 哲学           幸福论、人生意义、伦理、逻辑法则
│   │   ├── 思维方法       逻辑推理、批判性思维、模型思维、决策框架
│   │   ├── 科学           进化论、物理学、复杂性科学、数学思维
│   │   ├── 科技           半导体、通信技术、计算机网络、算法
│   │   ├── 自我提升       个人成长、习惯养成、效率工具、精力管理
│   │   ├── 商业           产品策略、市场竞争、营销、创新理论
│   │   └── 历史           以史为鉴、历史事件、文明演变
│   ├── comparisons/       对比分析
│   ├── questions/         已回答的好问题
│   ├── reading-list/      阅读清单追踪
│   └── meta/              仪表盘、lint 报告
│
├── _templates/            Obsidian Templater 模板
└── _attachments/          图片和附件
```

---

## 核心规则

1. **raw/ 是只读的。** 绝不修改、删除或移动 raw/ 下的任何文件。
2. **wiki/ 是你的。** 你全权负责创建、更新、重命名、删除 wiki/ 下的所有文件。
3. **每个 wiki 页面必须有 YAML frontmatter。** 无例外。
4. **使用 Wikilinks。** `[[页面名]]` 而非 `[text](path/to/file.md)`。文件名必须唯一。
5. **原子化笔记。** 一个概念一个页面。如果一个页面覆盖两个主题，拆分它。
6. **更新而非重复。** 如果一个概念已有页面，更新它而不是创建新页面。
7. **中文为主。** 页面标题、正文、注释使用中文。人名/专有名词保留原文或中英对照。
8. **双向链接。** 在页面 A 添加对 B 的链接时，检查 B 是否也应该链接回 A。
9. **保持页面精简。** 每页 100-300 行。超长页面需要拆分。

---

## Frontmatter 规范

### 通用字段（每个页面必须有）：
```yaml
---
type: source|concept|entity|domain|comparison|question|meta
title: "人类可读标题"
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags:
  - 标签1
  - 标签2
status: seed|developing|mature|archived
related:
  - "[[其他页面]]"
sources:
  - "[[raw/books/某本书笔记.md]]"
---
```

### 类型特有字段：

**source 类型**（书/课程摘要）：
```yaml
source_type: book|course|article|podcast
author: "作者名"
status: reading|finished|to-read
rating: 1-5（可选）
key_takeaways:
  - "要点1"
  - "要点2"
```

**concept 类型**（概念/思想）：
```yaml
complexity: basic|intermediate|advanced
domain:
  - 经济学
  - 心理学
aliases:
  - 别名1
  - 别名2
```

**entity 类型**（人物/组织）：
```yaml
entity_type: person|organization
role: "角色描述"
field: "领域"
```

**question 类型**（已回答的问题）：
```yaml
question: "原始问题"
answer_quality: draft|solid|definitive
```

---

## 三种操作

### INGEST（摄入）

触发：用户将文件放入 raw/ 或粘贴内容。

流程：
1. 完整阅读素材。
2. 与用户讨论关键收获。（用户说"直接摄入"时跳过讨论。）
3. 在 `wiki/sources/` 创建素材摘要页。
4. 为每个重要人物/组织创建或更新 `wiki/entities/` 页面。
5. 为每个重要概念创建或更新 `wiki/concepts/` 页面。
6. 更新相关的 `wiki/domains/` 领域页。
7. 如果大图景有变化，更新 `wiki/overview.md`。
8. 更新 `wiki/index.md` —— 为所有新页面添加条目。
9. 更新 `wiki/hot.md` —— 刷新热缓存。
10. 在 `wiki/log.md` 顶部追加日志条目：

```markdown
## [YYYY-MM-DD] ingest | 《书名/素材名》
- 素材: `raw/分类/文件名.md`
- 摘要: [[书名摘要]]
- 新建页面: [[概念A]], [[概念B]], [[人物C]]
- 更新页面: [[领域页]], [[已有概念D]]
- 关键洞察: 一句话总结这次摄入最重要的新认识。
```

11. 检查矛盾。发现矛盾时在两个页面都用 callout 标记：
```markdown
> [!矛盾] [[页面A]] 声称 X，但 [[页面B]] 认为是 Y。
```

一次摄入通常影响 8-15 个 wiki 页面。

### QUERY（查询）

触发：用户提出任何问题。

流程：
1. 先读 `wiki/hot.md`（约500字，可能已包含答案）。
2. 读 `wiki/index.md` 找到相关页面。
3. 读 3-5 个相关页面（超过10个说明定位不够精准）。
4. 综合回答，用 wikilinks 引用来源。
5. 提供"将此回答保存为 wiki 页面"的选项。
6. 如果问题暴露了知识缺口，主动提出"关于 X 我了解不足，是否需要查找素材？"

### LINT（检查）

触发：用户说"检查 wiki"或每 15-20 次摄入后。

检查项目：
- 孤立页面（无入站链接的页面）
- 死链接（链接到不存在的页面）
- 过时信息（被新素材取代的旧观点）
- 缺失页面（被提及但未创建的概念页）
- 缺失交叉引用（应互相链接但未链接的页面）
- Frontmatter 缺失或不完整
- 空章节
- 矛盾标记（`[!矛盾]` callout）是否已解决

输出：`wiki/meta/lint-report-YYYY-MM-DD.md`。不要自动修复，先征求用户同意。

---

## 索引与日志

### wiki/index.md（主索引）
按分类列出所有 wiki 页面，每项包含：wikilink、一句话描述、可选元数据。
每次摄入后更新。

### wiki/log.md（时间线日志）
追加式（新条目在顶部）。格式：
`## [YYYY-MM-DD] 操作类型 | 标题`

用简单命令查看最近活动：
```bash
grep "^## \[" wiki/log.md | head -10
```

### wiki/hot.md（热缓存）
约500字的最近上下文摘要。每次摄入、重要查询、每次会话结束时更新。
这是缓存，不是日志。每次完整覆写。

### wiki/overview.md（全局概览）
整个知识库的执行摘要。当大图景变化时更新。

---

## 写作风格

- 陈述式，现在时。"X 采用 Y 方法"而不是"X 基本上是 Y"。
- 大量使用链接。每次提及 wiki 中已有的概念都加 wikilinks。
- 引用来源：`(来源: [[页面]])`
- 标记不确定性：`> [!待验证] 这个结论需要更多证据。`
- 标记矛盾：`> [!矛盾] [[页面A]] 声称 X，但 [[页面B]] 认为是 Y。`
- 标记关键洞察：`> [!关键洞察] 核心要点...`
- 标记知识缺口：`> [!缺口] 这个主题尚未充分探索。`

---

## 上下文窗口管理

- 先读 `hot.md`（~500 tokens）
- 再读 `index.md`（~1000 tokens）
- 按需读领域 `_index.md`
- 每次查询只读 3-5 个完整页面
- 用搜索做关键词定位，不要全文扫描
- 用 Edit 做精确编辑，不要重写整个文件来改一个字段
- 不要把 wiki 内容粘贴到对话中，除非用户要求。用 wikilink 引用。

---

## 领域标签体系

使用以下标准化标签（可根据需要扩展）：

- `#领域/经济学` `#领域/心理学` `#领域/管理学` `#领域/认知科学`
- `#领域/哲学` `#领域/科学` `#领域/思维方法` `#领域/自我提升`
- `#类型/概念` `#类型/人物` `#类型/书籍` `#类型/课程`
- `#状态/种子` `#状态/发展中` `#状态/成熟` `#状态/待读`

---

## 迁移专用：Wolai 批量导入

当从 Wolai 迁移内容时：
1. 将 Wolai 导出的知识卡片放入 `raw/cards/`
2. 将 Wolai 导出的读书笔记放入 `raw/books/`
3. 批量摄入时每 10 张卡片向用户确认一次方向
4. 优先保持卡片的原子性（一张卡片通常对应一个 concept 页面）
5. 卡片之间的关联关系用 wikilinks 重建

---

## 文件命名规范

- **文件名**：使用中文，可带空格（`损失规避.md`）
- **目录名**：使用小写英文加连字符（`wiki/reading-list/`）
- **标签**：使用中文，层级用斜杠（`#领域/经济学`）
- **文件名唯一**：确保 wikilinks 无需路径即可工作
